{% if menu.children is defined and menu.children|length > 0 and menu.select == true %}
    <ul class="nav">
        {% for node in menu.children %}
            {% include 'MFBCmsBundle:Menu:sub.html.twig'
                with {'node': node}
            %}
        {% endfor %}
    </ul>
{% endif %}